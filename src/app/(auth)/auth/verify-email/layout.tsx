import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Verify Email | Buy Ghana Lands",
  description: "Verify your email address to complete your Buy Ghana Lands account registration.",
};

export default function VerifyEmailLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
